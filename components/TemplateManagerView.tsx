import React from 'react';

// This component is obsolete and was replaced by dedicated manager views.
// It is kept here as a placeholder to prevent import errors in case of stale references.
const TemplateManagerView: React.FC = () => {
    return null;
};

export default TemplateManagerView;
